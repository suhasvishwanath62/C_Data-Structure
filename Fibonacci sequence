#include <stdio.h>
#include <stdlib.h>

int fib(int n)
{
if(n==0)
    return 0;
if(n==1)
    return 1;
else
    return (fib(n-2)+fib(n-1));
}
main()
{
    int n;
    printf("enter the value\n");
    scanf("%d",&n);
    printf("fibonacci is = %d",fib(n));
}
